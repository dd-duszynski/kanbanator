export {
   templatesGetAll,
   templateGetSingle
} from './templates'

export {
   getBoards,
   getSingleBoard,
   createBoard,
   deleteBoard
} from './boards'

export {
   authStart,
   authSuccess,
   authFail,
   signupSuccess,
   setUserBoards,
   logout,
   auth,
   sign,
   setAuthRedirectPath,
   authCheck
} from './auth'
